# Assign a character
A <- "ABC"
print(class(A)) # Output: "character"

# Assign a numeric
A <- 123
print(class(A)) # Output: "numeric"

# Assign a logical (boolean)
A <- TRUE
print(class(A)) # Output: "logical"